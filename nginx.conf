# nginx config for Princeps frontend
# Serves static Vite build and proxies API requests to the backend
# BACKEND_HOST is substituted at container startup via envsubst

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Proxy API routes to backend
    location /api/ {
        proxy_pass http://BACKEND_PLACEHOLDER:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Proxy chat/SSE routes
    location /chat/ {
        proxy_pass http://BACKEND_PLACEHOLDER:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # Proxy WebSocket connections
    location /ws/ {
        proxy_pass http://BACKEND_PLACEHOLDER:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
    }

    # Proxy all other backend routes (matching vite.config.js proxy list)
    location ~ ^/(site|sites|tiles|opt|docs|grid|grid-efficiency|nom|planning|inventory|analytics|tenders|health|electricity|job|jobs|geeflow|procurement|legacy|geoai|bess|bipv|classify|prospector|scoring|nged|eso|repd|workflows|energy_system|tracker|rag|notifications|hardware|teaser|alerts|vision|retrofit)(/|$) {
        proxy_pass http://BACKEND_PLACEHOLDER:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Static assets with cache headers
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # SPA fallback — serve index.html for all unmatched routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
